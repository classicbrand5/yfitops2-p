import PlaceholderPage from './PlaceholderPage';
import { BarChart2 } from 'lucide-react';

export default function Analytics() {
  return (
    <PlaceholderPage
      title="Analytics"
      description="Commit frequency charts, build success rates, AI usage breakdowns, and terminal session analytics — all from live Supabase RPC queries. Coming in Phase 15."
      icon={BarChart2}
      accent="#9B6EF5"
    />
  );
}
